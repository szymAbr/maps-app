import styled from "styled-components";

const Main = styled.main`
  padding: 1rem 0;
`;

export default function Address() {
  return (
    <Main>
      <h2>Select route</h2>

      <h4>Start</h4>

      <h4>Finish</h4>
    </Main>
  );
}
